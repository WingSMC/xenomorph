{
    "name": "xenomorph",
    "scripts": {
        "build": "nr build:xeno && nr build:extensions",
        "build:extensions": "cd ./extensions/vscode && nr build && cd ../..",
        "build:xeno": "cargo build --release",
        "install:coverage": "cargo install cargo-llvm-cov && rustup component add llvm-tools",
        "install:deps": "cd ./extensions/vscode/client && ni && cd ../../..",
        "install:once": "npm install -g pnpm @antfu/ni && nr install:deps && nr install:coverage",
        "run:parser": "cargo run --release --package xenomorph_parser",
        "test": "nr test:xeno && nr test:extensions",
        "test:coverage": "cargo llvm-cov",
        "test:coverage:html": "cargo llvm-cov --html --output-dir coverage && echo 'Report: coverage/html/index.html'",
        "test:extensions": "cd ./extensions/vscode && nr test && cd ../..",
        "test:xeno": "cargo test --release"
    }
}

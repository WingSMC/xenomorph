import parser/p1;
type TestTuple = [UserName<string>, UserAge, email, bool];

type UserType = {
    name: UserName<string>,
    age: UserAge,
    email: email,
    isActive: bool,
    meta: TestTuple,
};

type UserList = UserType[] @minlen(1) @maxlen(1000);


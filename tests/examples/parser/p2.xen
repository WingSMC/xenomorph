import p1;

type UserName = string /^[A-Z][a-z]+$/ @minlen(3) @maxlen(50);
type UserAge = u8 @min(0) @max(150);
type UserType = {
    name: UserName,
    age: UserAge,
    email: email,
    isActive: bool,
};

type UserList = User[] @len(1<.<1000);

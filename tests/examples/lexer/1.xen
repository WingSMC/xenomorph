type Color = [
	"red",
	"green",
	"blue",
];

type User = {
	name: string,
	age: number,
	favoriteColor: Color,
} @Entity @Schema;

type ActionEntry = [Date, Action];

type Admin = User + {
	actions: ActionEntry[] @minlen(1) @maxlen(10),
};

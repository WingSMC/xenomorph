type Colors = [
    "red",
    "green",
    "blue",
];

type Color = { r: u8, g: u8, b: u8 };
type BrightChannel = u8 @min(125) @max(255);
type BrightColor = {
    r: BrightChannel,
    g: BrightChannel,
    b: BrightChannel,
};

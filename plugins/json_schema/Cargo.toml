[package]
name = "xenomorph_json_schema"
version = "0.1.0"
edition = "2021"

[package.metadata]
msvc-def-file = true

[lib]
crate-type = ["cdylib"]

[dependencies]
xenomorph_common = { path = "../../common" }
serde_json = "1.0"

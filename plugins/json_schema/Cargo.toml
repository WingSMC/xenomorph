[package]
name = "xenomorph_json_schema"
version = "0.1.2"
edition = "2021"

[package.metadata]
msvc-def-file = true

[lib]
crate-type = ["cdylib"]

[dependencies]
xenomorph_common = { path = "../../common" }
num-bigint = "0.5.1"
serde_json = { version = "1.0", features = ["arbitrary_precision"] }
